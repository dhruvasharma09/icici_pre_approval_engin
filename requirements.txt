# ICICI Pre-Approval Engine — Dependencies
# No external libraries required — uses Python 3.8+ stdlib only.
# (math, dataclasses, enum, typing are all stdlib)

# Optional: for API wrapper
# fastapi>=0.110.0
# uvicorn>=0.29.0
# pydantic>=2.0.0
